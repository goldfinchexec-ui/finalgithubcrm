# Firebase App Hosting configuration for Flutter Web
runConfig:
  outputDirectory: build/web

env:
  - variable: FLUTTER_VERSION
    value: "3.24.0"

# Explicitly specify the buildpack to use
buildpacks:
  - id: google.flutter
    version: latest
